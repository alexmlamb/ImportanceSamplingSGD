

To do :

1) implement Model API
    - use pickle to store the parameters
    - worker_process should be able to support the following config
        - different dataset
        - momementum, rmsprop
        - learning rate, mini-batch size
        - num hidden_layer, num of units per hidden layers
        - intialilization of weights

